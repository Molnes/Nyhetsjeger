package layout_components

templ QuizLayout(pageTitle string) {
	@BaseLayout(pageTitle) {
		<main
			id="main"
			class="bg-gray-100 w-full min-h-dvh"
		>
			<div class="w-full md:w-3/4 lg:w-1/2 max-w-screen-2xl mx-auto h-full py-4 px-6">
				{ children... }
			</div>
		</main>
	}
}
