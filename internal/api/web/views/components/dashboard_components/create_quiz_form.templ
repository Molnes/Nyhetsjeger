package dashboard_components

// The form wrapper for the "create quiz" page form elements.
// Used for layout.
templ CreateQuizForm(post string, children ...interface{}) {
	<form hx-post={ post } class="w-full mb-10">
		{ children... }
	</form>
}
